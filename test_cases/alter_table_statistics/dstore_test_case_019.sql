-- ALTER TABLE `{TEST_TABLE_NAME}` STATS_PERSISTENT=0, STATS_AUTO_RECALC=1, STATS_SAMPLE_PAGES=36;
-- STATS 组合 persist=0 recalc=1 pages=36

-- @PREPARE_START
DROP TABLE IF EXISTS `{{TEST_TABLE_NAME}}`;
CREATE TABLE `{{TEST_TABLE_NAME}}` LIKE `{{BASE_TABLE_NAME}}`;
INSERT INTO `{{TEST_TABLE_NAME}}` SELECT * FROM `{{BASE_TABLE_NAME}}`;
-- @PREPARE_END

-- @TIMER_START
ALTER TABLE `{TEST_TABLE_NAME}` STATS_PERSISTENT=0, STATS_AUTO_RECALC=1, STATS_SAMPLE_PAGES=36;
-- @TIMER_END
