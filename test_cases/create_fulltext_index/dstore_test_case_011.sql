-- CREATE FULLTEXT INDEX `idx_ft_tinytext` ON `{{TEST_TABLE_NAME}}` (`tinytext_col`);
-- 测试单列 tinytext CREATE FULLTEXT INDEX的执行情况

-- @PREPARE_START
DROP TABLE IF EXISTS `{{TEST_TABLE_NAME}}`;
CREATE TABLE `{{TEST_TABLE_NAME}}` LIKE `{{BASE_TABLE_NAME}}`;
INSERT INTO `{{TEST_TABLE_NAME}}` SELECT * FROM `{{BASE_TABLE_NAME}}`;
-- @PREPARE_END

-- @TIMER_START
CREATE FULLTEXT INDEX `idx_ft_tinytext` ON `{{TEST_TABLE_NAME}}` (`tinytext_col`);
-- @TIMER_END
